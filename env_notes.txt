The experiments will run three configurations on each of the two datasets.

We use the following models / APIs:


Deepseek v3.2
Deepseek official API key (set via DEEPSEEK_API_KEY environment variable):
<your_deepseek_api_key_here>

GLM 5.0
Zhipu official API key (set via GLM_API_KEY environment variable):
<your_glm_api_key_here>

Via the SiliconFlow service we call the following models:
Pro/MiniMaxAI/MiniMax-M2.5
Pro/moonshotai/Kimi-K2.5


SiliconFlow API key (set via SILICONFLOW_API_KEY environment variable):
<your_siliconflow_api_key_here>